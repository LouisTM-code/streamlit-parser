"""Интерфейсы HTTP-слоя.

Роль и ответственность:
- Определяет контракт HTTP-клиента для получения HTML-контента по URL.

Границы:
- Не выполняет сетевые запросы сам по себе.
- Не содержит реализаций, конфигурации транспорта и политики ретраев.

Взаимодействие с другими ролями:
- Используется инфраструктурными реализациями HTTP-клиента.
- Используется прикладными сервисами и парсерами через абстракцию.
"""

from typing import Protocol


class HttpClient(Protocol):
    """Протокол HTTP-клиента для получения HTML.

    Что делает:
    - Предоставляет единый метод получения HTML-документа по URL.

    Что возвращает:
    - Строку с HTML-содержимым успешного ответа.

    Какие ошибки допустимы:
    - Может пробрасывать исключения сетевого уровня,
      исключения таймаута и ошибки некорректного URL.
    """

    def get_html(self, url: str) -> str:
        """Возвращает HTML по указанному URL."""
